# Reference inspection notes

The attached reference is 1879 × 268 pixels with a 7.011:1 landscape aspect ratio. It is a dark-mode crop of the top of mestri. The header sits near the top edge, followed by a thin horizontal rule and a white hand-drawn signal trace. The `/01 AI SERVICE AVAILABILITY` line sits immediately below that trace with only a small, intentional breathing gap. The headline begins soon after the label and is already entering the lower edge of the crop. The main requested fix is therefore a smaller hero top offset, not a change to the trace itself.

Tiles 1 and 2 confirm the trace stretches across the page, with a slight bend/loop on the left and a shallow rise toward the center-right. The `/01` label is left-aligned with the content frame; the right-side companion label is aligned to the opposite edge.

Tiles 3 and 4 confirm the line continues as a broad, shallow wave and exits before the right margin. The live-status and theme controls stay anchored in the header at the upper-right. The `/01` companion text is vertically aligned with the left label, confirming the top content block should move upward as a whole while keeping the trace/header spacing intact.
